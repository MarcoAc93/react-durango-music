import { styled, FormControlLabel } from "@mui/material";

export const Label = styled(FormControlLabel)(({
  marginLeft: 0,
  '.MuiButtonBase-root': {
    paddingLeft: 0,
    paddingRight: 20
  }
}));
